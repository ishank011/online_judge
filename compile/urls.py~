from django.conf.urls import url

from . import views

urlpatterns = [
url(r'^submit',views.submitted, name='submitted'),
    url(r'^$', views.submit, name='submit'),
url(r'^successful',views.getval,name='successful'),
]

